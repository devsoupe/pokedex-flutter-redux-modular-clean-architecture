name: app_pokedex
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  feature_pokedex:
    path: ../../features/feature_pokedex
  domain:
    path: ../../domain
  data:
    path: ../../data
  core:
    path: ../../core
  common:
    path: ../../common
  design:
    path: ../../design

  cupertino_icons: 1.0.8

  get_it: 8.0.3
  injectable: 2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 5.0.0

  injectable_generator: 2.7.0
  build_runner: 2.4.15

flutter:
  uses-material-design: true