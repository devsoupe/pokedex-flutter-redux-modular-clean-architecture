name: pokedex
description: "pokedex-flutter-redux-modular-clean-architecture"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 5.0.0
  melos: 6.3.2

flutter:
  uses-material-design: true